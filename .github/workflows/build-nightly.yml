on:
  workflow_dispatch:
  push:
    branches:
      - "master"

permissions:
  contents: write
name: Build Nightly
jobs:
  ota-testbench:
    uses: ./.github/workflows/ota-testbench.yml
  build:
    needs: ota-testbench
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v5
        with:
          fetch-tags: true
          fetch-depth: 0
          submodules: 'recursive'
      - uses: actions/cache@v5
        with:
          path: |
            ~/.cache/pip
            ~/.platformio/.cache
          key: ${{ runner.os }}-pio
      - uses: actions/setup-python@v6
        with:
          python-version: '3.11'
      - name: Install PlatformIO Core
        run: pip install --upgrade platformio
      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: 'npm'
          cache-dependency-path: web/package-lock.json
      - name: Add version
        run: |
          mkdir -p out
          mkdir -p upload/display
          mkdir -p upload/headless
          mkdir -p upload/controller
          git tag -d nightly || true
          git tag -d db || true
          git describe --tags --dirty --exclude nightly --exclude db > out/version.txt || echo "v0.0.${GITHUB_RUN_NUMBER}" > out/version.txt
      - name: Build Web
        run: ./scripts/build_webui.sh
      - name: Build controller
        run: |
          export PLATFORMIO_BUILD_FLAGS="'-DNIGHTLY_BUILD'"
          pio run -e controller
          cp .pio/build/controller/firmware.bin out/board-firmware.bin
          cp .pio/build/controller/partitions.bin out/board-partitions.bin
          cp .pio/build/controller/bootloader.bin out/board-bootloader.bin
          cp .pio/build/controller/firmware.bin upload/controller/firmware.bin
          cp .pio/build/controller/partitions.bin upload/controller/partitions.bin
          cp .pio/build/controller/bootloader.bin upload/controller/bootloader.bin
      - uses: ./.github/actions/upload-firmware
        env:
          UPDATE_SERVER_HOST: ${{ secrets.UPDATE_SERVER_HOST }}
          UPDATE_SERVER_API_KEY: ${{ secrets.UPDATE_SERVER_API_KEY }}
        with:
          channel: 'nightly'
          target: 'controller'
          firmware_dir: 'upload/controller'
      - name: Build display FS
        run: |
          pio run -t buildfs -e display
          cp .pio/build/display/littlefs.bin upload/display/filesystem.bin
          cp .pio/build/display/littlefs.bin upload/headless/filesystem.bin
          cp .pio/build/display/littlefs.bin out/display-filesystem.bin
          cp .pio/build/display/littlefs.bin out/display-headless-filesystem.bin
      - name: Build display
        run: |
          export PLATFORMIO_BUILD_FLAGS="'-DNIGHTLY_BUILD'"
          pio run -e display
          cp .pio/build/display/firmware.bin out/display-firmware.bin
          cp .pio/build/display/partitions.bin out/display-partitions.bin
          cp .pio/build/display/bootloader.bin out/display-bootloader.bin
          cp .pio/build/display/firmware.bin upload/display/firmware.bin
          cp .pio/build/display/partitions.bin upload/display/partitions.bin
          cp .pio/build/display/bootloader.bin upload/display/bootloader.bin
      - uses: ./.github/actions/upload-firmware
        env:
          UPDATE_SERVER_HOST: ${{ secrets.UPDATE_SERVER_HOST }}
          UPDATE_SERVER_API_KEY: ${{ secrets.UPDATE_SERVER_API_KEY }}
        with:
          channel: 'nightly'
          target: 'display'
          firmware_dir: 'upload/display'
          firmware_files: 'bootloader.bin,partitions.bin,firmware.bin,filesystem.bin'
      - name: Build headless
        run: |
          export PLATFORMIO_BUILD_FLAGS="'-DNIGHTLY_BUILD'"
          pio run -e display-headless
          cp .pio/build/display-headless/firmware.bin out/display-headless-firmware.bin
          cp .pio/build/display-headless/partitions.bin out/display-headless-partitions.bin
          cp .pio/build/display-headless/bootloader.bin out/display-headless-bootloader.bin
          cp .pio/build/display-headless/firmware.bin upload/headless/firmware.bin
          cp .pio/build/display-headless/partitions.bin upload/headless/partitions.bin
          cp .pio/build/display-headless/bootloader.bin upload/headless/bootloader.bin
      - name: Build LilyGo T8 headless
        run: |
          export PLATFORMIO_BUILD_FLAGS="'-DNIGHTLY_BUILD'"
          pio run -e display-headless-t8
          cp .pio/build/display-headless-t8/firmware.bin out/display-headless-t8-firmware.bin
          cp .pio/build/display-headless-t8/partitions.bin out/display-headless-t8-partitions.bin
          cp .pio/build/display-headless-t8/bootloader.bin out/display-headless-t8-bootloader.bin
      - uses: ./.github/actions/upload-firmware
        env:
          UPDATE_SERVER_HOST: ${{ secrets.UPDATE_SERVER_HOST }}
          UPDATE_SERVER_API_KEY: ${{ secrets.UPDATE_SERVER_API_KEY }}
        with:
          channel: 'nightly'
          target: 'headless'
          firmware_dir: 'upload/headless'
          firmware_files: 'bootloader.bin,partitions.bin,firmware.bin,filesystem.bin'
      - name: Archive Firmware Files
        uses: actions/upload-artifact@v4
        with:
          name: gaggimate-firmware
          path: "out/*"
  release-nightly:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pages: write
      pull-requests: write
    steps:
      - name: Checkout repo
        uses: actions/checkout@v5
        with:
          # The changelog step runs `git log <previous>..<sha>`, which needs tags and history.
          fetch-tags: true
          fetch-depth: 0
      - name: Download Firmware Files
        uses: actions/download-artifact@v4
        with:
          name: gaggimate-firmware
          path: release
      - name: Generate changelog
        id: changelog
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          VERSION="$(cat release/version.txt)"
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"
          PREVIOUS="$(gh api "repos/${GITHUB_REPOSITORY}/releases/latest" --jq .tag_name || true)"
          {
            echo "Nightly build **${VERSION}**, use the nightly channel in the UI to download this firmware."
            echo
          } > notes.md
          if [ -z "${PREVIOUS}" ]; then
            exit 0
          fi
          # Same generator the stable release uses; the nightly tag still points at the previous build, so name the new one.
          if gh api --method POST "repos/${GITHUB_REPOSITORY}/releases/generate-notes" \
              -f tag_name="${VERSION}" -f target_commitish="${GITHUB_SHA}" -f previous_tag_name="${PREVIOUS}" \
              --jq .body > generated.md; then
            sed "s#/compare/${PREVIOUS}\.\.\.${VERSION}#/compare/${PREVIOUS}...nightly#" generated.md >> notes.md
          else
            echo "Changelog generation failed, see the commit history since ${PREVIOUS}." >> notes.md
          fi
          # The generator only lists merged pull requests; add what was pushed to master directly.
          COMMITS="$(git log "${PREVIOUS}..${GITHUB_SHA}" --no-merges --format='* %s (%h)' | grep -vE '\(#[0-9]+\) \([0-9a-f]+\)$' || true)"
          if [ -n "${COMMITS}" ]; then
            {
              echo
              echo "## Commits without a pull request"
              echo "${COMMITS}"
            } >> notes.md
          fi
      - name: Update Nightly Release
        uses: andelf/nightly-release@c5ed4bdb7c1da04a4fa1e40bc5e67306f682563b
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: nightly
          name: 'Nightly Release ${{ steps.changelog.outputs.version }} ($$)'
          prerelease: true
          body_path: notes.md
          files: |
            release/*
      - name: Deploy to GitHub Pages subdirectory
        uses: peaceiris/actions-gh-pages@4f9cc6602d3f66b9c108549d475ec49e8ef4d45e
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./release
          destination_dir: nightly
          publish_branch: gh-pages
      - name: Assemble flasher site
        run: |
          mkdir -p site/manifests
          cp flasher/index.html site/index.html
          cp flasher/manifests/*.json site/manifests/
          python3 -c "open('site/boot_app0.bin','wb').write(b'\xff'*8192)"
      - name: Deploy flasher to GitHub Pages
        uses: peaceiris/actions-gh-pages@4f9cc6602d3f66b9c108549d475ec49e8ef4d45e
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./site
          destination_dir: flash
          publish_branch: gh-pages
